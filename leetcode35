class Solution {
    public int searchInsert(int[] nums, int target) {
        int i=0;
        while(nums[i]<target)
        {
            if(i>=nums.length-1)
            return ++i;
        
         if(nums[i]==target)
                return i;
                else
                 i++;
        }
        return i;
    }
}
